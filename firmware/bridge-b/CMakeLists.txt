cmake_minimum_required(VERSION 3.16)
set(EXTRA_COMPONENT_DIRS "${CMAKE_CURRENT_LIST_DIR}/../common")
add_compile_definitions(BRIDGE_ROLE_B=1)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(bridge_b)
